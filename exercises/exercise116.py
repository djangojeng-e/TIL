# Tuple unpacking refers to unpacking values into the variables.

# e.g.

nums = (1, 2, 3)
a, b, c = nums

print(a)
print(b)
print(c)

# We can see the values in tuples have been unpacked and
# assigned to each variables a, b, c