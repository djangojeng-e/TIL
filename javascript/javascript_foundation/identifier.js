// Identifier 식별자

// 어떤 이름을 식별할수 있는 형태로 작성

var name = 'Mark';
function hell() {

};

var person = {name: 'Mark', age: 37};

// Identifiers are case sensitive

var myName = 'Mark';
var myname = 'Mark';

// these are different

// identifiers can be unicode $, - 0 - 9 but it cannot start with numbers 0 - 9
// reserved words and empty characters cannot be used


var name1;
var $name;
var _name;

var 이름 ; // this is possible but use English!
